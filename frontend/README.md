# RegnRate Static Site

A React + Vite static frontend project that regenerates a folder-style name on demand.

## Available commands

- `npm install` - install dependencies
- `npm run dev` - start local development server
- `npm run build` - build production files
- `npm run preview` - preview the production build

## Usage

Open the site in the browser, then click the button to regenerate the folder name.
